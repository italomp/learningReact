
/**
 * Component responsável por avaliar as credenciais
 * que serão usadas para o login.
 * Retorna true, caso estejam ok.
 * Retorna false, caso contrárrio.
 */
const Auth = () =>{
    return true;
} 

export default Auth;